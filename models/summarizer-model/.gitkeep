# Summarizer Model Directory

Place DistilBART-ONNX or T5-small-ONNX model files here for AI-powered summarization.

Required files:
- model.onnx
- tokenizer.json
- config.json (optional)

This directory is currently empty. The app uses extractive summarization as fallback.
